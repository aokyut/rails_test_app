<%= form_with(model: kiji, local: true) do |form| %>
  <% if kiji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kiji.errors.count, "error") %> prohibited this kiji from being saved:</h2>

      <ul>
      <% kiji.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
